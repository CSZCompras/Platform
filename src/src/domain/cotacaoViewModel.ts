import { MarketViewModel } from "./marketViewModel";

export class CotacaoViewModel{

    id                      : string;
    buyListName             : string;
    markets                 : MarketViewModel[]; 
    observation             : string;
}